# TO-DO-APP
A lightweight To-Do app that lets users add and manage daily tasks with ease. It uses the browser’s Storage API (LocalStorage) to save data, so tasks remain available even after refresh or reopening. Users can mark tasks as completed with a single click, making it simple to track progress and stay organized.
